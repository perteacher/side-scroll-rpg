# progress — 메이플×그라나도 RPG 리부트

## 반복 1 (2026-09-14) — 전체 자가검수 시작
- git 저장소 초기화, 초기 커밋 56605c7 (30파일 / 5,276줄).
- 기획서 완료기준 9개 + 이후 추가 기능 15개를 테스트 항목으로 변환.
- 검수 방식: 실제 브라우저(localhost:8421)에서 window.__game을 구동해 단언 검사.
  로직이 DOM(ui.js)과 얽혀 있어 headless Node 시뮬레이션 대신 브라우저 내 구동 선택.

### 테스트 범위 (총 88개 단언)
T1 캐릭터 생성(9) · T2 이동/점프(4) · T3 공격/발사체(4) · T4 스탠스·무기 연동(7)
T5 창 토글(6) · T6 NPC 상호작용(5) · T7 병영 교체(4) · T8 자동전투 3모드(4)
T9 2층 발판(4) · T10 워프/분기맵(7) · T11 보스 패턴(9) · T12 사망/부활(5)
T13 물약(5) · T14 세이브/로드(12) · T15 드랍·상점·제작·강화(7) · T16 퀘스트/시나리오(4)
T17 쿨다운 UI(5) · T18 성능(3)

### 하네스 결함 (게임 버그 아님, 테스트 쪽 수정)
- update()만 직접 호출하면 input.endFrame()이 안 돌아 눌린 키가 영구 유지됨.
  → 점프 직후 무한 재점프처럼 보였다. 실제 루프와 동일한 tick()(update+endFrame)으로 교체.
- 점프 체공이 46프레임인데 40프레임만 돌려 "착지 실패"로 오판.
- 보스 체력바/쿨다운 오버레이 검사에서 activeUnit이 아닌 슬롯의 DOM을 조회해 오판.

### [고침] 마을 안에서 전멸하면 부활하지 않음
- 증상: 파티 전원 HP 0 → "마을로 돌아갑니다" 로그는 뜨는데 downed가 안 풀림.
  이후 물약 사용·조작이 모두 막혀 게임이 사실상 멈춤.
- 원인: _checkDowned가 _teleport(가장 가까운 마을)를 호출하는데, 이미 그 마을에 있으면
  travelTo()가 false로 조기 반환 → 그 뒤의 _reviveAll()까지 통째로 건너뜀.
- 수정: 목적지가 현재 존과 같으면 _teleport 대신 _reviveAll()을 직접 호출. (game.js _checkDowned)
- 재실행: 88/88 통과.

## 스모크 수치 (납품 기록)
- 프레임 처리시간: 2.06ms/frame (update+render, 예산 16.6ms). 통과.
- 최악 장면 오브젝트: 23개 (5티어 존 + 파티 3 + 보스/발사체/이펙트). 여유 충분.
- 이펙트 상한 240개 유지 확인.
- 콘솔 에러: 0건 (부팅 → 생성 → 전투 → 보스 → 전멸/부활 → 세이브/로드 전 구간).

## 반복 2 — 세이브 초기화 버그
### [고침] 세이브 초기화 버튼이 동작하지 않음
- 증상: 초기화(🔄)를 눌러도 리로드 후 기존 세이브가 그대로 살아남아 생성화면이 안 뜸.
- 원인: clear() 직후 reload()가 즉시 언로드하지 않아, 그 사이 rAF 프레임의 _tickAutosave와
  beforeunload 핸들러가 방금 지운 세이브를 다시 써버렸다. (자동저장 타이머 초기값이
  undefined라 첫 프레임에 바로 저장되는 것도 겹쳤다)
- 수정: game.resetting 플래그를 세우고, SaveManager.save() 진입부에서 한 번에 차단.
  호출 지점(자동저장/존이동/수동저장/beforeunload)을 개별로 막지 않고 저장 함수 한 곳에서 막음.
- 재실행: 회귀 36개 단언 전부 통과, 초기화 → 생성화면 복귀 확인.

## 최종 상태
- 단언 88 + 회귀 36, 실패 0.
- 콘솔 에러 0건 (클린 부팅 / 플레이 / 초기화 전 구간).
- 발견·수정한 실제 버그 2건: 마을 내 전멸 시 부활 누락, 세이브 초기화 무효화.

## 반복 3 — 가문/레벨/퀘스트 확장 검수
- 가문 특성 19개 단언 통과. 트리 조건(윗단계 투자+가문레벨), 연쇄 회수, 저장 복원 확인.
- 일반 퀘스트 22개 단언 통과. 수주→사냥/수집/납품→보상, 반복 수행, 세이브 복원 확인.
- 시나리오 확장 20개 단언 통과. 49단계 전 챕터 완주, 납품 NPC 클릭 경로까지 확인.

### 검수 중 드러난 테스트 절차 함정
- localStorage를 직접 지우고 reload하면 beforeunload/자동저장이 다시 써서 이전 세이브가
  살아난다. 초기화는 반드시 ui.onResetSave()(resetting 플래그 경로)를 써야 한다.
  이 때문에 "승급 카드 등급 제한" 테스트가 이전 세션의 고레벨 캐릭터로 돌아 오판했다.

### [추가] 누락돼 있던 저장 항목
- 가문 레벨/경험치/특성 투자, 캐릭터 레벨 감지 기준선(_seenLevels), 일반 퀘스트 진행·완료 횟수.
  기준선을 저장하지 않으면 로드 때마다 가문 경험치가 중복 지급된다.

## 반복 6 — 무기 세트 / 캐릭터 개성 / 심연의 탑 / 사운드 (2026-09-14)

추가한 것 4가지. 단언 140개 작성, 전부 통과.

### ① 무기 세트(장비교체등록)
- UI 골격만 있던 탭을 실제 기능으로 구현. 무기 3세트 등록 → 1/2/3 키로 교체.
- equipment.weapon1/weapon2를 weaponSets[activeSet]에 연결한 접근자로 바꿨다.
  이렇게 하니 장착/해제/장비보너스/스탯계산/세이브 등 기존 코드를 손대지 않아도 세트가 따라온다.
- [고침] refreshPartyHUD가 매 프레임 .name-lv의 textContent를 덮어써서 세트 표시(세트1/2/3)가
  즉시 지워졌다. 테스트로 잡음. innerHTML로 마크업째 다시 쓰도록 수정.
- [고침] 해고 시 unit.equippedList()만 회수해서 비활성 세트의 무기가 사라졌다. allGear()로 교체.
- [고침] 핫바 그리드가 3열 고정이라 전용기 버튼이 둘째 줄로 밀렸다. 4열로.

### ② 캐릭터 고유 특성 · 전용기
- 영입 35 + 클래스 5 = 40명 전원에게 패시브 1 + 전용기 1. 패시브 12종, 전용기 7유형.
- 가문 보너스(unit.family)를 unit.bonus로 이름을 바꾸고 가문+특성+버프를 합산하도록 정리.
  이름이 '가문'인데 특성·버프까지 담기면 읽는 사람이 오해한다.
- EMPTY_FAMILY_BONUS에 defPct/crit/critDmg/lifesteal 추가, mergeBonuses() 도입.
- 성능 주의: 보너스 합산을 전 유닛(38명)이 아니라 파티 3명만 매 프레임 계산하도록 했다.

### ③ 심연의 탑 (엔드게임)
- 존 하나를 재사용하며 층마다 내용물을 갈아 끼운다. noRespawn 플래그를 zone.js에 추가해야
  "층을 다 비웠다" 판정이 성립했다(기존엔 12초 뒤 리스폰).
- 탑 보스는 층마다 수치가 달라야 해서 BOSS_DATA(이름 기준) 대신 인스턴스에 bossData를 붙였다.
  ai.js는 boss.bossData || BOSS_DATA[boss.name] 순으로 본다.
- 전멸 처리: _nearestTownIndex()가 zm.index 기준이라 탑(마지막 인덱스)에서 죽으면 항상 town6로
  갔다. 기준 존을 인자로 받도록 고쳐 입장 전 존 기준으로 귀환한다.
- 탑 도전은 저장하지 않고 최고 기록만 저장한다. 탑에서 저장된 게임은 가까운 마을에서 재개.

### ④ 사운드
- 음원 파일 없이 Web Audio API로 합성. 오실레이터 + 노이즈 버퍼 + 바이쿼드 필터.
- 자동재생 정책 때문에 첫 키/클릭 전에는 컨텍스트를 못 연다. unlock()을 window 리스너로 걸고,
  그 전에는 모든 호출이 조용히 무시되도록 ready 가드를 뒀다.
- 타격음은 한 프레임에 수십 번 울릴 수 있어 종류별 최소 간격(45~120ms)을 뒀다.
- BGM은 setInterval(25ms) 룩어헤드 스케줄러로 8분음표를 예약한다. setInterval만으로 음을 내면
  박자가 흔들린다.
- 음소거 상태에서는 노드를 아예 만들지 않는다(테스트로 확인: 노드 생성 0).

## 스모크 수치
- ms/frame: 탑 28층(잡몹 12) 0.89 / 최다 스폰 사냥터(몹 25) 0.83. 예산 16.6ms. 통과.
- 콘솔: 부팅 → 생성 → 사냥 → 탑 → 보스 → 전멸 → 세이브/로드 → 초기화까지 에러·경고 0건.
- 단언 140/140 통과.

## 반복 7 — 디테일 보강 (2026-09-14)

실제로 열어보고 찾은 누락 2개 + 체감 개선 5개 + 편의 2개. 단언 83개 추가(누적 223개), 전부 통과.

### 누락이었던 것
- [고침] 인벤토리(I)에 장비가 아예 안 나왔다. 드랍된 무기/방어구는 pm.gear에 들어가는데
  refreshInventory는 pm.items만 훑었다. 장비 보관함 절을 추가하고 장착·판매 버튼을 붙였다.
- [고침] 영입이 끝난 NPC도 머리 위 '!'를 계속 띄웠다. 마을마다 영입 NPC가 7명이라 누가 남았는지
  알 수가 없었다. 상태를 4가지(!/…/?/✓)로 나눠 표시한다.

### 체감 개선
- 장비 비교: 보유 장비 옆에 지금 낀 것 대비 ▲+12 / ▼-5를 보여준다.
- 획득 표시: 드랍이 채팅에만 남아서 자동사냥 중엔 뭐가 나왔는지 몰랐다. 몹 자리에서 떠오르게 했다.
  데미지 숫자보다 느리고 오래(1.5초) 떠 있어야 읽힌다.
- 몹 레벨: 존 권장 레벨을 몹에 물려주고 이름 앞에 Lv.N을 붙였다. 내 레벨과의 차이로 색이 바뀐다.
- 목표 트래커: 화면 우측에 시나리오/영입/의뢰 목표와 탑 진행을 상시 표시.
- 자동 물약: HP/MP가 기준선 아래로 떨어지면 자동으로 먹는다. 0.8초에 한 번, 한 명씩만.

### 편의
- 설정 창(O): 음량 슬라이더, 자동물약 on/off와 기준선, 표시 토글, 조작 안내, 플레이 기록.
  설정은 세이브와 별개로 localStorage에 남는다(캐릭터를 새로 만들어도 유지).
- 플레이 기록: 처치/보스처치/누적골드/플레이시간/전투불능/방문지역. 세이브에 포함.
  누적 골드를 세려고 pm.gold += 를 전부 pm.addGold()로 모았다.

### 이번에 잡은 UI 버그
- [고침] 상단 아이콘이 12개가 되면서 한 줄을 넘겨 두 번째 줄이 화면을 가렸다. 34px → 30px.
- [고침] 설정 창의 기록/조작표가 3열 그리드(캐릭터 스탯용)를 쓰다 보니 항목 이름이 줄바꿈됐다.
  설정 창 전용으로 2열 규칙을 따로 줬다.
- [성능] 트래커를 매 프레임 문자열로 다시 만들 이유가 없어 250ms 주기로 낮췄다(0.003ms).

## 스모크 수치 (반복 7)
- ms/frame: update 0.42 + render 0.72 = 1.14. 예산 16.6ms. 통과.
- 콘솔: 부팅 → 생성 → 사냥 → 탑 보스 → 설정/인벤 → 세이브/로드 → 초기화까지 에러·경고 0건.
- 단언 83/83 통과 (누적 223).

## 반복 8 — 창 겹침 해소 (2026-09-14)

증상: 스탯창·장비창·퀘스트창이 전부 화면 한가운데(top/left 50% + translate)에 고정돼 있어
여러 개를 열면 완전히 포개졌다. 아래 창은 존재조차 안 보였다.

- 창 매니저를 ui.js에 추가했다. openWindow가 숨김을 푼 뒤 크기를 재서 빈 자리를 찾는다.
  좌(10,62) / 우(오른쪽 끝-10, 62) 두 자리가 기본. 겹치는지는 실제 사각형으로 판정한다.
- 화면이 960x540이라 420x390 창은 둘까지만 안 겹친다. 셋째부터는 열려 있는 창 수만큼
  계단식으로(28px, 26px씩) 비껴 놓아 제목 표시줄이 항상 보이게 했다.
  [고침] 처음엔 후보 목록의 마지막 값으로 떨어져서 셋째와 넷째가 같은 자리에 겹쳤다.
- 제목 표시줄을 끌어 옮길 수 있다. 채팅창에만 있던 드래그를 일반화해 모든 창에 붙였다.
  버튼(닫기/최소화) 위에서 시작한 드래그는 무시한다.
- 끌어다 놓은 자리는 설정(localStorage)에 남아 새로고침해도 유지된다. 채팅창도 포함.
  설정 창에 "창 위치 초기화" 버튼을 뒀다.
- 클릭한 창이 앞으로 온다(z-index 증가).
- 창 최대 높이를 460 → 390px로 줄였다. 상단 바(56) 아래 62px에서 시작해 하단 파티 슬롯 위에서
  끝나게 하려면 이 높이여야 한다. 내부 컨테이너가 전부 overflow-y:auto라 잘리는 곳은 없다(확인함).

## 스모크 수치 (반복 8)
- 창 4개를 동시에 열어 위치 중복 0건, 첫 두 개 겹침 0건.
- 드래그 후 새로고침 → 위치 복원 확인. 화면 밖으로 끌어도 경계에 물린다.
- 긴 목록(장비 30개 + 잡템 25종)에서도 창이 화면 밖으로 안 나가고 내부 스크롤로 처리됨.
- 콘솔 에러 0건. 단언 22/22 통과 (누적 245).

## 반복 9 — 전체 도트 그래픽 전환 (2026-09-15)

비교본(①현재 / ②도트 필터 약 / ③도트 필터 강 / ④직접 찍은 도트)을 실제 게임 화면으로 만들어 보여주고
④로 결정. 필터는 캔버스 글자가 뭉개지고(②) 그라데이션이 줄무늬로 깨졌다(③).

### 구조
- 스프라이트 = 문자열 격자 + 팔레트. compileSprite가 오프스크린 캔버스로 만들고 키별로 캐시한다.
  이미지 파일이 없어 exe 용량도 그대로다.
- 아이템: 모양 38개 × 팔레트로 123종. 티어는 금속/가죽 장식/옷감 색으로 표현.
  UI 목록(인벤·장비·세트·상점·제작·강화·게시판)에 data URL 아이콘, 드랍 연출에도 아이콘.
- 캐릭터: 16x24 레이어 합성(+머리 위 4px 여유). 외형은 캐릭터 id 해시로 고정.
- 몬스터: 종족 5틀 × 이름 해시 색. NPC는 캐릭터 틀 재사용.
- 렌더러 전면 교체: 월드는 480x270 버퍼에 그려 2배 확대, 좌표는 2px 격자에 맞춘다(snapPx).
  글자·체력바·이펙트 글자는 원래 해상도 층. EffectManager.draw를 drawShapes/drawTexts로 나눴다.

### 잡은 문제
- [고침] 머리색 고르다 예외: (seed >> 5) % n 이 음수. >>는 부호를 살린다. >>>로 바꾸고 pickBySeed로 모음.
- [고침] 활 시위 글자 e가 팔레트에 없어 안 그려짐. 검증 스크립트로 "팔레트에 없는 글자" 전수 검사해 찾음.
- [고침] 가죽 두건이 도넛처럼, 신발이 막대기처럼 보였다. 두건은 끝을 뾰족하게, 신발은 좌우 대칭으로
  뒤집지 않고 두 짝을 같은 방향으로 다시 찍었다(대칭이면 발끝이 마주 본다).

### 스모크 수치 (반복 9)
- 모양 전부 16x16/16x24 규격, 팔레트 누락 0, 아이템 123/123 렌더.
- ms/frame: update 0.10 + render 0.30 = 0.40 (이전 1.14). 캐시된 스프라이트 40장.
- 필드·마을·탑 보스(예고 표시)·전투불능 장면 렌더 확인, 콘솔 에러 0건. 회귀 단언 11/11 (누적 260).

## 반복 10 — 단계별 스탠스 + 스탠스 레벨 스탯 성장 (2026-09-15)

- 스탠스에 weapon(계열)·grade(bare/base/advanced/master)를 붙였다. 무기 아이템은 계열만 가지므로
  상위 스탠스는 새 무기 없이 같은 무기로 쓴다. availableStances = 해금된 스탠스 중 장착 무기 계열과 맞는 것.
- 상위 스탠스 20종은 기본 스탠스에서 생성(사거리·배율 상향 + 전용 스킬 17종). 스킬은 요구레벨 1/3/5 순 배치를
  전 스탠스에 대해 단언으로 확인.
- 해금: 베테랑=첫째 무기 상급 / 익스퍼트=둘째 무기 상급 / 마스터=첫째 무기 마스터. tierStartLevel은 LEVEL_TIERS에서.
- 스탯: unit.stats를 getter로 바꿨다. 기본치 + Σ(스탠스 레벨-1)×계열 성장치×등급 배율. 계산 결과는 캐시하고
  스탠스 레벨이 바뀔 때만 invalidateStats로 비운다(데미지 계산마다 불리므로 캐시 필수).
- 상위 스탠스는 고레벨 사냥터에서 키우므로 경험치 요구를 ×300 / ×15,000. 안 곱하면 베테랑 사냥터 몹 2마리에
  상급 스탠스가 만렙이 된다(계산: 몹 경험치 4.2만 × 0.6 = 스탠스 2.5만/킬, 상급 25레벨 누적 요구 1.4만).
- [고침] 손에 든 무기 표시가 gear.stanceId === currentStanceId로 비교해서 상위 스탠스로 바꾸면 무기가 사라졌다.
  스탠스의 weapon 계열과 비교하도록 수정.
- 테스트 함정: 원거리 스킬은 투사체가 날아가 명중할 때 피해가 들어간다. 시전 직후 체력 비교는 0으로 나온다.

## 스모크 수치 (반복 10)
- 단언 28/28 (누적 288). 콘솔 에러 0건.

## 반복 11 — 메이플스토리 시스템: 이동 / 장비 성장 / 수집·계정 성장 (2026-09-15)

### 이동
- 로프는 존을 불러올 때 발판마다 자동 생성(x는 2px 격자). 로프 위에서는 물리를 건너뛰고 로프 조작만 받는다.
- ↑가 점프와 로프 잡기를 겸한다. 로프 앞(발판 아래)에서만 잡고, 발판 위 로프 자리에서는 잡지 않게 해서
  2층에서 점프가 막히지 않도록 했다(_ropeAt은 발판 높이보다 아래일 때만 참).
- 리더 전환 직후 이전 리더가 로프에 매달린 채 남지 않게 동료 갱신에서 onRope를 푼다.
- 바닥 전리품: 파티원 누구든 150px 안이면 빨려와 주워진다. 자동사냥 중 원거리 동료가 잡은 몹의 전리품도
  주워지게 하려는 범위. 60초 / 존 이동 시 사라진다.

### 장비 성장
- 강화(+10)·인챈트를 스타포스·잠재능력으로 교체. 확률은 시도 전체 기준으로 성공 / 파괴 / 실패를 나눈다.
- 잠재능력 수치는 unit.bonus와 단위를 맞췄다(비율 0.03, 크리티컬은 퍼센트포인트). 주스탯 줄은 stats getter에,
  크리·HP%는 equipmentBonus에, 나머지 비율은 personalBonus에 — 같은 옵션을 두 번 세지 않게 한 곳씩만.
- 장비를 끼거나 빼거나 세트를 바꾸거나 강화·큐브를 쓰면 invalidateStats로 스탯 캐시를 비운다.
- 착용 중인 장비가 파괴되면 방어구 슬롯/무기 세트 어디에 있든 찾아서 벗긴다(removeGearEverywhere).
- 테스트는 Math.random을 고정값으로 바꿔 성공/유지/하락/찬스타임/파괴/방지/스타캐치 경계를 전부 짚었다.

### 수집·계정 성장
- 몬스터 컬렉션의 계정 스탯은 전역 ACCOUNT_FLAT_STATS + 버전 번호로 둔다. 캐릭터 stats 캐시는 버전이
  바뀌면 스스로 다시 계산하므로, 병영에 있는 캐릭터나 나중에 영입한 캐릭터도 따로 챙길 필요가 없다.
- 링크 스킬은 같은 특성끼리 겹치지 않고 최고 레벨만. 계정 공용 보너스(가문+링크+컬렉션 마일스톤)를 한 번
  합친 뒤 캐릭터별 보너스와 더한다.

## 스모크 수치 (반복 11)
- 단언: 이동·전리품 19 + 스타포스·잠재능력·컬렉션·링크·세이브 45 + UI 11 = 75 (누적 363). 콘솔 에러 0건.

## 반복 12 — 상태이상 / 해상도 변경 (2026-09-16)

### 상태이상
- 데이터(js/data/status.js)와 로직(js/engine/status.js)을 나눴다. 대상에 statuses 맵을 붙이는 방식이라 몹·파티원이 같은 함수를 쓴다.
- 도트 피해는 "건 순간의 피해 × 비율"로 고정해 둔다. 틱마다 다시 계산하지 않아 방어 붕괴·감전이 도트에 이중으로 붙지 않는다
  (감전의 받는 피해 증가는 applyDamageToEnemy 한 곳에서만 곱한다).
- 원거리·마법 스킬은 투사체에 statuses를 실어 두고 명중할 때 굴린다. 범위 스플래시도 같은 목록으로 굴린다.
- [고침] 스플래시 피해가 회피(miss)를 무시하고 0 피해를 박던 것 — miss를 넘겨 MISS로 표시.
- 보스 무한 기절 방지: 기절·빙결 35% 길이 + 6초 면역.
- 검수 함정: 레벨 30 파티를 레벨 100 존에 둔 채 수동 틱을 길게 돌리니 전멸 → 마을 귀환으로 존이 바뀌어 뒤 테스트가 전부 헛돌았다.
  매 틱 파티 HP를 채우고 주변 몹을 비선공으로 바꾼 뒤 다시 돌려 통과. 또 확률 굴림을 0.99로 고정한 채 투사체 비행 틱을 돌려
  30%·50% 상태이상이 안 걸린 것도 테스트 쪽 실수였다(명중은 틱 중에 일어난다).

### 해상도
- 게임 좌표는 960x540 고정. #game-root 전체에 transform scale, 캔버스는 CSS 960x540에 실제 픽셀만 배율만큼 → 렌더러는
  기본 변환만 setTransform(outScale)로 바꾸면 나머지 그리기 코드가 그대로 동작한다. 미니맵도 같은 방식.
- 마우스 좌표를 쓰는 곳은 캔버스 클릭과 창 끌기·배치·위치 저장 두 군데뿐이라, 둘 다 getBoundingClientRect를 배율로 나눠 논리 좌표로 바꿨다.
- 숨김 탭·최소화에서 창 크기가 0으로 잡혀 캔버스가 240x135로 줄던 것 → 크기 0이면 적용을 건너뛴다.
- 데스크톱판: preload로 setContentSize 하나만 열었다(contextIsolation 유지).

## 스모크 수치 (반복 12)
- 단언: 상태이상 55(부여·중첩·도트·피해 증가·방어 붕괴·냉기·보스 면역·투사체·스킬 3계열·도트 처치 보상·파티 기절/화상/저항·UI·렌더)
  + 해상도 19(배율·캔버스 픽셀·중앙 정렬·미니맵·클릭 좌표·창 끌기·위치 저장·해상도 옵션·렌더) = 74 (누적 437). 콘솔 에러 0건.

## 반복 13 — 배경·이펙트 도트 전환 / 강화 개편 / MENU / 편의성 (2026-09-16)

### 배경·이펙트
- js/data/sprites_scenery.js를 새로 만들어 배경도 아이템·캐릭터와 같은 방식(픽셀 격자 + 팔레트)으로 찍는다.
  모양은 한 번만 찍고 존 테마의 far/mid/accent 색을 팔레트로 갈아끼워 14개 테마에 돌려 쓴다.
- 하늘 그라데이션 → 9단 색 띠(mixHex), 먼 산 삼각형 → 4px 단 계단 산맥 + 하늘색 혼합(대기 원근),
  장식은 칸마다 해시로 위치·좌우·크기를 흔들고 가끔 하나를 비워 반복을 감춘다.
- 지면 자갈은 카메라가 아니라 월드 좌표를 해시에 넣어 스크롤해도 제자리에 있게 했다.
- 이펙트는 arc/gradient를 걷어내고 점(4~6px 사각형)으로 호·고리를 그린다. 곡선이 섞이면 도트 화면에서 혼자 매끄러워 보인다.

### 강화 개편 (사용자 요청)
- 명칭을 "스타포스"에서 "강화"로 되돌리고, 스타캐치(움직이는 별을 멈춰 성공률 ×1.05)를 들어냈다.
  버튼 한 번으로 바로 굴린다. 확률·파괴·찬스 타임·파괴 방지는 그대로.

### MENU (사용자 요청)
- 상단에 늘어서 있던 아이콘 13개를 MENU 버튼 하나로 접었다. 펼치면 이름과 단축키가 함께 보인다.
- 고르면 닫히고, 바깥을 누르거나 Esc로도 닫힌다. 소리 버튼만은 음량을 여러 번 돌릴 수 있게 열어둔다.

### 편의성
- 고급 HP/MP 물약·만병통치약 추가. 자동 물약은 많이 비었을 때(40% 미만) 고급부터 쓴다 — 좋은 물약을 반만 채우고 버리지 않게.
- 아이템 설명 풍선: 목록 종류가 많아 행마다 붙이지 않고 document 위임 + data-tip-item / data-tip-gear 속성으로 처리.
  마우스 좌표는 화면 배율로 나눠 960x540 기준으로 되돌려 놓는다.
- 채팅 탭 8개 → 실제 쓰는 4개(전체·시스템·파티·NPC).
- 파티 이름표: 앞 순서의 가까운(46px) 동료 수만큼 위로 띄워 겹침 해소.
- 동료도 로프를 탄다. [고침] 목표 높이에 ±8px 여유를 두니 발판 2px 아래에서 방향이 0이 되어 매달린 채 멈췄다
  → 리더가 발판 위면 끝까지 오르고 바닥이면 끝까지 내리도록 방향을 고정하고, 끝에서 2px 안이면 내려서게 했다.
- 멀리 처진 동료(추종 거리 1.8배)는 점프 → 플래시 점프로 따라붙는다.

## 스모크 수치 (반복 13)
- 단언: MENU·강화·물약 15 + 툴팁·채팅·동료 9 + 동료 로프 재검사 6 = 30 (누적 467). 콘솔 에러 0건.

## 반복 14 — 초반 10분 설계 (2026-09-16)

### 왜
"게임이 재미없다"는 지적을 초반 10분 문제로 좁혔다. 실제 수치를 보니 시작 후 하는 일은
델가도와 대화 → 숲도적 8마리였고, 레벨업은 HP 회복뿐이며, 이 게임의 핵심인 캐릭터 40명 수집은
Lv.1 화면 어디에도 드러나지 않았다. 조사한 공통 원칙(첫 5~10분이 재방문을 가른다 / 와우 모먼트 /
30~90초마다 피드백 / 변동 보상에 확정 보상 섞기)에 맞춰 다섯 가지를 넣었다.

### 넣은 것
- 프롤로그 "영웅의 기억": 첫 실행에 Lv.110 영웅 3인으로 마왕전. 자막·레터박스·건너뛰기(Esc).
  전투 기록·드랍·컬렉션·세이브를 전부 건너뛰도록 _checkEnemyDeath / _rollDrops / _tickAutosave에 가드를 뒀다.
- 버닝(Lv.20 미만 레벨업 +1), 레벨 보상 상자 7단계(보상 + 다음 할 일 한 줄), 최초 처치 확정 드랍,
  3티어 이상 드랍 화면 번쩍임 + 전용 효과음, 캐릭터 도감 35명(미보유는 실루엣), 1티어 영입 조건 완화.

### 고친 것
- [고침] 프롤로그가 update() 도중에 끝나면 그 프레임의 나머지가 빈 파티로 계속 돌아
  _tryUseWarp에서 예외가 났다 → 종료된 프레임은 즉시 return, 워프·카메라에도 파티 없음 가드.
- [고침] 파티가 없는 상태에서 자동 저장이 돌면 빈 세이브가 남아 "첫 실행" 판정이 흐려졌다
  → SaveManager.save가 프롤로그 중이거나 파티가 없으면 저장하지 않는다.
- 검수 함정: 숨겨진 미리보기 창에서는 rAF가 멈춰 전투가 진행되지 않는다. 수동 틱으로 다시 측정했다.
  또 이전 테스트에서 만든 파티가 beforeunload로 다시 저장돼 프롤로그가 건너뛰어졌다 → resetting 플래그까지 세우고 재현.
- 보스가 11초 만에 죽어 연출이 짧았다 → 체력 150k → 320k (20초 남짓).

## 스모크 수치 (반복 14)
- 단언: 버닝·레벨보상·드랍·영입 15 + 캐릭터 도감 8 + 프롤로그 27 = 50 (누적 517). 콘솔 에러 0건.

## 반복 15 — 쿼터뷰(2.5D) 전환 (2026-09-16)

### 왜
"라피스 같은 2.5D로 바꿀 수 있나"는 요청. 겉모습만 바꾸는 선택지도 제시했지만 완전 쿼터뷰 전환을 골랐다.

### 무엇을 갈아엎었나
- 좌표계: y가 '높이'에서 '남쪽'으로 바뀌었다. 중력·점프·발판·로프·플래시 점프를 전부 들어냈다.
- iso.js 신설: 투영(sx = x-y, sy = (x+y)/2), 타일맵(32px 칸, 22줄), 벽 충돌(축 분리), 배치 보정(nearestFree).
- tiles.js 신설: 마름모 바닥 타일 4종을 도트로 찍고 존 색으로 팔레트만 바꾼다. 길은 흙색으로 확실히 구분.
- renderer.js·ai.js는 새로 작성. 렌더는 타일 → x+y 정렬 렌더로, AI는 평면 거리·8방향 이동으로.
- game.js: 이동·전리품·투사체·타겟 탐색·카메라·클릭 역투영을 평면 기준으로 교체.
- 스프라이트: 뒷모습 레이어(머리·머리카락·투구·옷)를 추가해 앞/뒤 × 좌우반전 = 4방향.

### 그대로 남은 것
스탯·스탠스·스킬·상태이상·아이템·강화·잠재·영입·퀘스트·컬렉션·링크·세이브·UI 창 전부.
존 데이터도 그대로 쓰고, 1차원 x 배치만 평면으로 옮겼다(2층에 있던 대상은 길 뒤쪽 줄에).

### 고친 것
- [고침] 카메라가 맵 밖으로 나가 허공이 화면을 채우던 것 → 맵 가장자리에서 멈추게.
- [고침] 워프 진입키가 이동키(↑)와 겹쳐 지나가기만 해도 이동되던 것 → Enter로 분리.
- 검수 함정: 숨겨진 미리보기 창에서는 rAF가 멈춰 자동전투가 진행되지 않는다(수동 틱으로 측정).

## 스모크 수치 (반복 15)
- 단언: 이동·충돌·전투 7 + 워프·마을 3 + 방향 스프라이트 6 + 전체 회귀(전 존 로드·마을 상호작용·사냥·상태이상·세이브 왕복) 12
  + 심연의 탑 6 = 34 (누적 551). 콘솔 에러 0건.

## 반복 16 - 마을 배치 설계 (2026-09-16)

쿼터뷰로 바꾼 뒤 마을이 사냥터와 같은 뼈대(가운데 길 + 흩어진 장식)라 마을 같지 않았다. 마을만 따로 짰다.

- TileMap을 _buildField / _buildTown으로 나눴다. 마을은 큰길 + 가운데 광장 + 길 양쪽 집 줄로 배치한다.
- 마을 가구 도트 8종 추가: 우물, 좌판, 울타리, 통, 상자, 표지판, 가로등, 천막.
- 사람 자리를 배치에서 미리 정한다(map.npcSpots). 상점/게시판/시나리오 NPC는 광장, 영입 NPC는 집 앞.
  zone.js가 이 자리를 순서대로 나눠주고, 막힌 칸이면 nearestFree로 밀어낸다.
- [고침] 가구가 테마 색(풀색)으로 칠해져 바닥에 묻혔다. 돌/나무 팔레트를 따로 줬다(PROP_PALETTE_KIND).
- 테마별 건물: 숲/국경 마을은 집, 항구는 집과 배, 사막은 천막, 성채는 집과 성벽.

## 스모크 수치 (반복 16)
- 단언: 마을 가구/광장/사람 자리/사냥터 회귀 8 (누적 559). 콘솔 에러 0건.

## 반복 17 - 몬스터 4방향 / 난이도 재조정 (2026-09-16)

### 몬스터 4방향
- 종족 5종(인간형/짐승/언데드/마족/무생물)의 뒷모습 몸통을 따로 찍었다. 다리는 좌우 대칭이라 공용.
- monsterSprite(enemy, frame, dir)로 방향을 받고, 이동 방향이 화면 위쪽이면 등을 보인다.

### 난이도 (측정 먼저)
권장 레벨 파티(레벨에 맞는 티어 장비 + 스탠스 성장)를 만들어 존마다 재 봤다.

  측정 전: Lv1 몹 HP 40 / 파티 DPS 363 -> 처치 0.11초
           Lv45 몹 HP 420 / 파티 DPS 6,302 -> 처치 0.07초   (한 프레임 만에 죽는다)
  원인: 몹 수치는 사이드스크롤 시절 값 그대로인데 파티에만 스탠스 성장/강화/잠재/컬렉션/링크가 쌓였다.

- balance.js에 레벨별 배율표(HP/공격/경험치/방어)를 두고 Enemy 생성 때 곱한다. world.js는 손대지 않았다.
- 조정 후 실측 TTK: Lv1 1.4초 / Lv10 1.2초 / Lv20 2.2초 / Lv35 3.4초 / Lv45 3.4초 / Lv100 2.9초 / Lv110 3.3초.
  30초 연속 사냥 기준 분당 8~20마리, 분당 1~4레벨. Lv45에서는 파티원 한 명이 쓰러질 만큼 위험도가 생겼다.
- [고침] 상위 존에서 몹 방어력(640~2,600)이 파티 공격력(약 700)을 넘어 피해가 1로 주저앉았다.
  피해 공식에 하한을 뒀다: 원 피해의 15%는 방어력과 무관하게 들어간다.
- 경험치는 ×3(상위 존 ×1.2~2)만 올렸다. 처치 속도가 2~3배 느려진 만큼만 보정해 성장 속도를 유지한다.
- 심연의 탑은 자체 층 배율이 있어 noScale로 빼고, 1층 기준값을 직접 올렸다(HP 60 -> 5,200).

## 스모크 수치 (반복 17)
- 단언: 배율표/피해 하한/상위 존 5 + 전체 회귀(존 로드, 몬스터 방향, 사냥, 탑, 세이브) 10 = 15 (누적 574). 콘솔 에러 0건.

## 반복 18 - 지형 높이차 / NPC 방향 (2026-09-16)

### 지형 높이차 (예전 2층을 쿼터뷰로)
- 타일맵에 height(0~2단)와 ramp 배열을 더했다. 사냥터 북쪽 여섯 줄이 2단 고지대다.
- 화면에서는 한 단당 12px 올려 그리고, 남쪽/동쪽 면에 흙빛 절벽을 세운다.
  절벽 벽은 '칸 경계선에서 위로' 자라기 때문에 앞 칸 바닥과 겹치지 않는다.
- 이동 규칙은 "한 번에 한 단까지". 2단 경계는 절벽이 되고, 1단으로 깎은 경사로(13칸 간격)로만 오른다.
- 높이가 두 단 차이면 서로 조준·추격하지 않는다(sameLevel을 sameHeight로 되살렸다).
- [고침] 경사로 바로 위에 나무가 서서 못 올라가는 자리가 있었다. 경사로 위아래로 흙길을 내고
  길 칸에는 장식을 세우지 않게 했다. 전 존 경사로 173개 전부 등반 확인.
- [고침] 리더가 고지대로 올라가면 동료가 절벽에 붙어 멈췄다. 높이가 다르면 가장 가까운 경사로를
  먼저 향하게 하고, 그래도 4초 넘게 못 따라오면 리더 옆으로 옮긴다(길찾기가 없는 게임의 흔한 처리).
- [고침] 같은 높이에 몹이 없으면 자동사냥이 멈췄다. 이 경우에도 경사로를 향해 이동한다.
- 미니맵에 고지대를 한 겹 밝게 칠했다.

### NPC 방향
- NPC는 걷지 않아서 방향이 없었다. 배치할 때 길 쪽을 보게 기본 방향을 정하고(homeFacing),
  파티가 140px 안에 들어오면 그쪽으로 돌아본다. 멀어지면 원래 방향으로 돌아간다.
- 렌더는 캐릭터와 같은 방식(앞/뒤 두 벌 + 좌우 반전)을 쓴다.

### 검수에서 헛짚은 것 (기록)
- 절벽이 너무 두껍게 그려진다고 판단해 한참 파고들었는데, 확인용으로 캔버스에 덧그린 확대 오버레이가
  프레임마다 겹쳐 쌓인 것이었다. 실제 채우기 좌표는 처음부터 정확했다.
  교훈: 화면을 건드리는 디버그 훅은 한 번 쓰고 반드시 되돌린 다음 다시 재라.

## 스모크 수치 (반복 18)
- 단언: 지형 높이(평지/고지대/경사로/절벽/추격/조준) 9 + 동료·자동사냥 경로 2 + NPC 방향 4 +
  전체 회귀(존 20곳 로드·렌더, 자동사냥, 세이브) 3 = 18 (누적 592). 콘솔 에러 0건.

## 반복 19 - 갈래길 / 장식 감소 / 스폰 증가 / 킵 AI 손질 (2026-09-17)

### 길을 갈래길로
- 예전엔 9~12줄을 통째로 칠한 일직선 띠였다. 이제 8칸마다 목표 줄(8~16줄)을 뽑아 이어 붙인
  구불구불한 본길을 깔고, 17칸마다 남쪽 샛길이 갈라져 빈터(원형)로 빠진다. 셋 중 하나는 다시 본길로 붙어 고리가 된다.
- 경사로마다 본길과 잇는 오르막 샛길을 내고, 고지대 위로도 길을 조금 더 이었다.
- 워프·파티 복귀·몹 배치는 이제 직선 줄이 아니라 roadRowAt(x)를 본다. 미니맵도 실제 길 타일을 찍는다.
- 전 존에서 서쪽 끝 → 동쪽 끝까지 길이 이어지는지 BFS로 확인(끊긴 존 0).

### 장식 감소
- 길에서 몇 칸 떨어졌는지를 BFS로 재고(_roadDistance) 그 값에 비례해 드물게 세운다.
- 존당 평균 175개 → 62개. 시야가 트이고 몹을 쫓다 나무에 끼는 일이 크게 줄었다.

### 몹 스폰 3배
- ZoneManager가 존 정의의 몹 목록을 불려서 쓴다(spreadEnemies, 한 종류당 3마리, x로 150px씩).
  보스와 소환몹은 그대로 둔다. field1a 기준 12 -> 36마리.

### 킵 사냥 버벅임 (요청)
- [고침] 고지대에서 킵 사냥이 절벽·나무에 막혀 제자리걸음했다.
  0.15초 이상 못 움직이면 좌우 중 canStand로 뚫린 쪽을 찔러 보고 0.5초간 옆으로 돈다.
  양쪽 다 막히면 뒤로 물러난다. 30초 측정에서 제자리걸음 프레임 14.4% -> 2.6%, 처치 6~9마리.
- [고침] 사냥감이 다른 단에 있으면 곧장 가다 절벽에 박았다. 이제 경사로부터 탄다.
  대상 선택에서도 같은 단 몹에 900px 가산점을 줘 먼저 고른다.
- 홀드 중인 동료가 리더와 560px 넘게 벌어지면 킵으로 자동 전환(대화창 알림).
- 따라붙기 시작 거리 96 -> 260px.

## 스모크 수치 (반복 19)
- 단언: 길 갈래·연결·장식·스폰 10 + 홀드/추적 거리 3 + 고지대 킵 사냥 2 + 세이브 1 = 16 (누적 608). 콘솔 에러 0건.

## 반복 20 - 레벨대 x 등급 장비 / 승급 (2026-09-17)

### 장비 개편
- 티어 5단계(40레벨 이후 정지) -> 레벨대 13단계 x 등급 5단계. 기본형 19종을 곱해 1,365종을 찍어낸다.
- 장신구(반지·목걸이) 부위를 새로 넣었다. 도트도 새로 찍었다(금속 틀 + 등급색 보석).
- 착용 제한(reqLevel)이 생겼다. 제작은 일반·고급까지, 희귀 이상은 사냥으로만.
- 보스는 장비를 반드시 하나 떨어뜨리고 등급 분포가 높은 쪽으로 쏠린다.
- [메움] 사냥터가 45~100 구간이 비어 있어서 그 레벨대 장비를 얻을 데가 없었다.
  탑 몹에게 층 기반 레벨(20 + 층x2)을 줘서 탑이 그 구간을 채운다.

### 승급
- 100레벨에서 경험치가 멈추고, 병영에서 재료+인장+골드를 내면 베테랑으로 승급한다. 이후 110/120에서 익스퍼트·마스터.
- 재료에 1티어(저레벨) 재료를 섞어 상위 존만 돌아서는 승급이 안 되게 했다. 인장은 보스 전용 드랍.
- 표시는 전부 rankLabel로 통일(텔레포트 "100+" -> "베테랑 Lv.1+", 몹 이름표·존 제목도 동일).
- 세이브에 rank를 추가. 예전 세이브는 레벨에서 등급을 되짚는다(rankFromLevel).

### 밸런스 (측정하며 다섯 번 고쳤다)
- [고침] 몹 공격이 파티 방어력을 완전히 무시했다(ai.js에서 enemy.atk를 그대로 뺐다).
  방어구가 장식이었고, 레벨대 장비가 커지자 100+ 존에서 4초 만에 전멸했다.
  -> 비율 감쇄(def / (def + 60xLv), 최대 75%)로 바꾸고, 최대 HP에 장비 방어력의 절반을 더했다.
- 100/110/120 배율을 다시 잡았다(HP x10/3/0.35, 공격 x0.55/0.4/0.3).
- 30초 자동사냥 실측(희귀 등급 기준): Lv1 58킬/분, Lv20 32, Lv40 26, Lv100 28, Lv110 30, Lv120 52.
  100+ 존에서 일반 등급으로는 전멸하고 희귀 이상이면 안정적 — 장비를 갖춰야 상위 존이 열린다.
- [주의] 세이브 호환: 사라진 아이템 id(구 _t2~_t5)는 로드할 때 버린다.

## 스모크 수치 (반복 20)
- 단언: 장비 체계 7 + 승급 10 + 세이브/로드 3 + 착용 제한·장신구 5 + 회귀 2 = 37 (누적 645). 콘솔 에러 0건.

## 반복 21 - 세이브 지우고 처음부터 플레이해서 밸런스 재조정 (2026-09-17)

세이브를 지우고 1레벨부터 자동사냥으로 끝까지 돌려 봤다(자동 장착/판매/제작/물약 구매/존 이동 포함).
지금까지의 밸런스는 '스킬·스탠스를 다 올린 파티'로 잰 값이라 실제 시작 파티와 맞지 않았다.

### 처음 플레이에서 드러난 것들
- 1회차: Lv16까지 3분. 경험치가 3배 배율 + 몹 3배라서 성장이 너무 빨랐다 -> 경험치 배율 3 -> 1.
- 2회차: 10~20레벨에서 9분 동안 18번 전멸. 원인 둘.
  (1) 11~20레벨대 장비 제작이 1~10존 재료(도적의 천조각)를 요구해서, 상위 존으로 가면 제작이 막혔다.
      -> 레벨대별 제작 재료를 그 레벨대에서 사냥하는 존의 재료로 바꿨다.
  (2) 무기가 10계열이라 쓸 수 있는 무기가 안 나왔다. -> 드랍률 7%->10%, 무기는 70% 확률로 파티 계열로.
- 3회차: 골드가 말라 제작·물약을 못 샀다. 경험치 배율을 내리면서 메소(경험치 비례)까지 같이 줄었던 것.
      -> 메소를 몹 레벨 기반(6 + 레벨x4)으로 분리. 탑에서 경험치가 폭증해도 골드는 안 터진다.
- 4회차: 그래도 10~20에서 전멸. 몹 HP 배율이 여전히 높았다 -> 1~45 구간 배율 35% 하향(15레벨 36->22 등).
- 5회차: Lv1->40을 24분에, 전멸 0회로 통과. 장비도 레벨대에 맞춰 따라왔다(40레벨에 백은 = 31~40 장비).

### 탑(45~100)
- 층당 배율이 1.28배라 9층에서 벽. 파티는 레벨만 오르고 장비는 층 레벨을 따라가서 뒤처졌다.
  -> 층당 HP 1.12 / 공격 1.09로 낮추고, 몹 레벨을 20+층x3으로 올려 25층=100레벨이 되게 맞췄다.
  -> 5분에 3~4층, 7~10레벨. 전멸 0회로 꾸준히 오른다.
- 탑 경험치 기준값 600 -> 150, 성장률 1.30 -> 1.16 (예전엔 5분에 19레벨이 올랐다).

### 그 밖에 고친 것
- [고침] 쓰러진 파티원이 마을에 가야만 일어났다. 한 명이 쓰러지면 경험치도 못 받고(쓰러진 유닛은 제외)
  그 사냥 내내 짐이었다. 실제로 탑에서 검사만 3레벨 뒤처졌다. -> 25초 뒤 절반 체력으로 자동 기상.
- 승급 재료 40% 감량, 골드 비중 상향(15만 -> 20만). 파티 3명이 전부 승급해야 해서 부담이 3배였다.
- 110/120 존 배율 재조정(HP x2.2/x0.3, 공격 x0.3/x0.25). 물약을 들고 가면 110은 사냥 가능,
  120은 익스퍼트 장비가 있어야 한다.

### 최종 실측 (자동사냥, 물약 구매 포함)
  Lv1->10  2.7분 / Lv10->20 6.3분 / Lv20->30 7.4분 / Lv30->40 7.7분  (전멸 0회)
  Lv45 이후 탑: 5분에 3~4층 + 7~10레벨, 24층 근처에서 100 도달
  100존 18킬/분(안정) · 110존 12킬/분(물약 소모 많음) · 120존은 장비 미달이면 버겁다

## 스모크 수치 (반복 21)
- 단언: 회귀·배율·탑·기상·승급 10 (누적 655). 콘솔 에러 0건. 새 세이브로 시작 확인.

## 반복 22 - NPC 상호작용 / 드랍 양 (2026-09-17)

### NPC를 어디를 눌러도 대화가 되게
- 원인: 클릭을 월드 좌표로 바꿔 발밑 상자(26x26)와 비교했다. 쿼터뷰라 그림은 발밑보다 한참 위에
  그려지므로, 몸통을 누르면 월드 좌표가 NPC 뒤쪽 땅으로 떨어져 아무것도 안 잡혔다.
- 고침: 화면 좌표도 같이 넘겨서, 그려진 사각형(사람 44x62 · 게시판 52x60 · 몹은 몸집 비례)으로 먼저
  판정한다. 발밑 상자는 보조로 남겼다. 몹 클릭(타겟 지정)도 같이 쉬워졌다.
- 추가: 86px 안에 NPC·게시판이 있으면 Space로 대화가 열린다(공격보다 우선). 대상 머리 위에
  'Space 대화'가 뜬다. 사냥터에서는 대상이 없으니 Space는 그대로 공격이다.

### 드랍이 너무 많다
- 재어 보니 처치당 1.6개(1분에 19개)가 바닥에 떨어졌다. 몹을 3배로 깔면서 같이 3배가 된 것이고,
  레벨대와는 무관했다(초반 존이라서가 아니다).
- 장비 10% -> 6%, 재료는 표 값에 0.55배, 메소는 빈도 0.7 -> 0.33에 한 번 양을 2.1배.
  골드 수입은 그대로 두고 바닥에 떨어지는 개수만 줄였다.
- 결과: 처치당 0.74개. 5분 신규 플레이 재측정에서 Lv13(전멸 0), 10분에 Lv21 · 숙련 장비로
  성장 속도는 그대로였다.

## 스모크 수치 (반복 22)
- 단언: 클릭 판정 6 + Space 상호작용 4 + 드랍/회귀 5 = 15 (누적 670). 콘솔 에러 0건.

## 반복 23 - 50~95 사냥터 / 병작(쩔) (2026-09-18)

### 중반 사냥터 10곳 + 마을 2곳
- 45에서 100까지 사냥터가 없어 탑만 돌아야 했다. 설원(50/55) · 화산(60/65) · 하늘(70/75) ·
  심연(80/85) · 신전(90/95)으로 다섯 티어를 채웠다. 마을은 카르네비아(50) · 아에리스(70).
- 테마 7종(frost/volcano/sky_isle/abyss/temple + 마을 2종)과 재료 6·7티어를 새로 만들었다.
  51~70 장비는 6티어 재료로, 71~100 장비는 7티어 재료로 만든다.
- 몹 수치는 45존과 100존 사이를 기하평균으로 이어 붙였고, 배율표에 50~95 구간 점을 넣었다
  (경험치 배율은 1로 두고 존 정의에 실제 값을 적었다).
- 실측(45레벨 · 흑철 장비 파티): 45 -> 64가 25분, 전멸 1회. 장비도 용린(51~60)으로 따라왔다.
- 탑 경험치를 낮췄다(기준 150 -> 70, 성장 1.16 -> 1.14). 예전 값이면 사냥터를 만들어도
  탑이 4배 효율이라 아무도 안 온다.

### 병작(쩔)
- 수련장 존을 만들었다. 들어갈 때마다 파티 최고 레벨로 몹을 다시 깐다(체력 40%/공격 55%/경험치 70%).
- 몹은 자기보다 15레벨 이상 낮은 파티원을 노리지 않는다(다른 상대가 있을 때만). 일반 존에서도 적용.
- 사냥 경험치에 한 마리당 상한(다음 레벨 요구량의 35%)을 뒀다. 100레벨 존에 1레벨을 세워도
  한 방에 몇 레벨씩 뛰지 않는다. 퀘스트·경험치 카드는 상한 없음.
- 실측: Lv60 둘 + Lv1 하나, 1분에 Lv1 -> Lv7, 저레벨 체력 100% 유지(한 대도 안 맞음).
- [고침] 수련장 안에서 다시 수련장으로 들어가면 travelTo가 같은 인덱스라 되돌아와 몹이 갱신되지 않았다.

## 스모크 수치 (반복 23)
- 단언: 새 존 9 + 병작 6 + 회귀 6 = 21 (누적 691). 콘솔 에러 0건.

## 반복 24 - 새 마을 영입 캐릭터 / 중반 게시판 의뢰 (2026-09-18)

- 50~95 구간을 만들면서 새 마을 둘이 빈 채로 남았다(영입 NPC 0명, 게시판 의뢰 0개).
- 영입 캐릭터 10명 추가(카르네비아 5 · 아에리스 5). 로스터 35 -> 45명.
  전용기·특성도 각자 붙였다(설벽 창진 / 빙하 붕괴 / 질풍 연사 / 뇌운 강림 / 전열 재정비 등).
- 영입 퀘스트 6·7티어를 만들었다. 새 존의 몹·재료·그 마을 NPC를 쓰는 5~6단계 체인이다.
- 게시판 의뢰 12개(카르네비아 5 · 아에리스 7). 50~95 존의 사냥·수집·보스 토벌을 다룬다.
- 검수: 퀘스트가 가리키는 존 id / 몹 이름 / 아이템 id / 보상 아이템이 실제로 있는지 전수 확인.
  영입 퀘스트의 talk 대상 NPC가 그 마을에 있는지도 확인. 전부 통과.

## 스모크 수치 (반복 24)
- 단언: 캐릭터·전용기 3 + 마을 배치 3 + 퀘스트 참조 검사 2 + 영입 진행 6 + 회귀 5 = 21 (누적 712).
  콘솔 에러 0건.

## 반복 25 - 병영 탭 분리 / 장비 자동 판매 (2026-09-18)

- [고침] 병영을 열면 승급 패널이 화면을 차지해 캐릭터 교체 목록이 안 보였다.
  승급 재료가 캐릭터마다 일곱 줄씩이라 창 밖으로 밀려났던 것.
  -> 창 안에 탭을 넣어 [캐릭터 교체] / [승급]으로 나눴다. 기본은 교체 탭.
- [고침] 탭 패널에 .hidden만 걸었더니 안 감춰졌다. .hidden 규칙이 .game-window / .tab-panel 에만
  걸려 있어서, 패널에 tab-panel 클래스를 같이 줬다.
- 장비 자동 판매(설정 O): 기준 등급 이하 장비는 주운 즉시 판다(기본 끄기).
  '지난 레벨대' 옵션은 파티 레벨대보다 낮은 장비를 등급과 무관하게 판다.
  안 줍는 방식 대신 줍고 파는 방식으로 했다 — 뭐가 떨어졌는지는 보이게 하려고.
  자동 장착은 넣지 않았다(고르는 재미를 남기려고).

## 스모크 수치 (반복 25)
- 단언: 병영 탭 4 + 자동 판매 5 + 회귀 4 = 13 (누적 725). 콘솔 에러 0건.

## 반복 26 - 장비 판매는 상점에서만 (2026-09-21)

- 가방(I)에서 바로 팔 수 있던 것을 막았다. 장비 보관함의 [판매] 버튼을 없애고,
  판매 처리(onSellGear)도 상점 창이 열려 있을 때만 받도록 잠갔다.
  -> 잡템은 원래 상점에서만 팔렸으니 이제 판매 창구가 상점 하나로 모인다.
- 가방 하단에 "장비·잡템 판매는 마을 상점에서만 할 수 있습니다." 안내를 붙였다.
  버튼만 사라지면 어디서 파는지 몰라 헤맬 수 있어서.
- 주운 즉시 파는 자동 판매(반복 25)는 pm.sellGear를 직접 부르므로 이 잠금과 무관하게 그대로 동작한다.

## 스모크 수치 (반복 26)
- 단언: 가방 판매 차단 4 + 상점 밖 호출 차단 2 + 상점 판매 정상 3 + 자동 판매 경로 1
  + 회귀(모든 창 / 잡템 / 30초 구동) 3 = 13 (누적 738). 콘솔 에러 0건.

## 반복 27 - 스탠스 전면 개편 / 스카우트를 힐러로 (2026-09-21)

- 스탠스를 원작(나무위키 '그라나도 에스파다/스탠스') 기준으로 전부 다시 만들었다. 11종 -> 37종
  (맨손 + 12계열 x 3등급). 이름만 바꾼 게 아니라, 원작에서 자세마다 다른 것들을 실제 수치로 옮겼다:
  평타 타수(hits), 평타 스플래시(splash), 자세 고유 보정(bonus).
  -> 아웃레이지 샷은 평타 4타, 폴암 계열은 기본 자세부터 평타가 광역, 익스퍼트급은 전부 면역/관통 보유.
- [주의] 타수를 늘리면서 총 배율이 뛰지 않게 basicAtkMult = power / hits 로 두고,
  기본 스탠스의 power는 예전 값 그대로 유지했다(밸런스가 이 값 기준으로 잡혀 있다).
- 스카우트를 힐러로 바꿨다. 평타가 치료이고, 퍼스트 에이드/포르티투도는 맨손으로도 쓴다.
  무기는 로자리오(새 무기 계열 + 아이콘). 상급 이상 자세는 로자리오를 껴야 열린다.
- attackType 'support'를 새로 만들고 ROLE_SKILLS_DATA.support 18종을 추가했다.
  kind로 하는 일이 갈린다: heal / healAll / buff / cleanse / refresh / revive.
  원작 프랜스펄(인핸스 택틱스 1번)은 공·방·공속·이속·저항·명중을 한 번에 올린다.
- 저항력(resist)을 보너스 항목으로 새로 만들었다. statusResistChance에 더해져 상태이상 확률을 깎는다.
- [고침] 무기가 주는 것은 '스탠스'가 아니라 '계열'이다. 힐러는 자세가 둘인데 무기는 로자리오 하나라
  canEquip/setStances/availableStances를 전부 계열 기준으로 고쳤다.
  안 고치면 장비교체등록 탭에서 STANCE_DATA['rosario']가 undefined라 창이 통째로 터졌다.
- [고침] 세이브 로드가 Object.assign으로 옛 stanceProgress를 통째로 덮어써서, 힐러가 된 스카우트에게
  옛 장궁 스탠스 레벨이 그대로 남아 스탯에 계속 더해졌다. 그 캐릭터가 실제로 가진 자세만 되살린다.
- [밸런스] 힐러가 평타로 때리지 않으니 파티 딜이 1/3 줄었다. 같은 harness로 재보니
  딜러3 파티가 4분에 Lv11인데 힐러 파티는 Lv9였다(Lv10 도달 3.0분 -> 4.3분).
  -> (1) 힐러가 할 일 없을 때 자세를 돌려 버프를 마저 걸게 하고(원작처럼 버프가 자세마다 따로 있다),
     (2) 포르티투도 계열 버프 수치를 올리고, (3) PARTY_XP_MULT = 1.4를 넣어 곡선을 되돌렸다.
     재측정: 1/2/3/4분에 Lv 5/7/9/11 — 딜러3 파티와 같은 속도이고 HP는 내내 90~100%다.
- 캐릭터 생성에서 셋 중 한 명은 반드시 스카우트(힐러)가 되도록 막았다. 마지막 한 자리까지 힐러가
  없으면 그 자리는 힐러만 받는다. 생성 카드에 '필수' 배지.

## 스모크 수치 (반복 27)
- 단언: 스탠스 개편 11 + 힐러 8 + 생성 규칙 3 + 전투/AI 9 + 단계 해금 4 + 회귀(모든 창 / 전 존 /
  세이브 / 세트 스탠스) 6 = 41 (누적 779). 콘솔 에러 0건.

## 반복 28 - 영입 퀘스트 개별화 / 길잡이·여정 / 하단 HUD 정리 (2026-09-22)

- [고침] 영입 퀘스트가 티어별로 한 벌뿐이라 같은 마을 일곱 명이 토씨 하나 안 틀리고 같은 시험을 냈다.
  -> 45명 전원에게 각자 다른 퀘스트를 줬다(자동 검사: 45/45 전부 다름). 인물마다 첫 대사도 따로 붙였다.
  단계 종류도 늘렸다: 기존 사냥/수집/대화/납품에 reach(정찰)와 pay(골드 지불)를 더했다.
- 시나리오 연계: 로렌초·카밀라·이사벨·로잘린에게 납품하는 퀘스트, 시나리오 보스(골모/크라켄/아르콘/
  발데/프로스타/모르간/아스테리온)를 잡아 오라는 퀘스트를 섞었다.
- 시나리오 게이트: 10명은 지정 챕터를 마쳐야 수락할 수 있다(마을은 이야기보다 먼저 갈 수 있으므로).
  퀘스트 창에 자물쇠와 이유가 뜬다.
- 스탠스 레벨업 채팅에서 오른 스탯 표기를 뺐다. 레벨과 스킬포인트만 알린다.

- 길잡이(튜토리얼) 12단계 + 여정 30과제를 새로 만들었다(js/data/journey.js, js/engine/journey.js).
  - [고침] 처음엔 단계를 순서대로만 인정했더니, 자동사냥만 돌리는 파티가 1번 "움직여 보기"에 묶여
    13마리를 잡고도 진행이 0이었다. -> 판정은 순서와 무관하게 하고 안내만 첫 미완료 단계를 가리킨다.
  - [고침] '다음 목표'를 진행 비율로만 고르니 6장 과제가 1장보다 먼저 떴다. 장 순서를 먼저 본다.
  - [고침] 여정 지표 '스탠스 레벨 합계'는 시작부터 차 있었다(맨손·미해금 자세까지 세어서).
    가장 많이 키운 자세 하나를 보는 것으로 바꿨다.
  - 진행도는 저장하지 않고 늘 현재 상태에서 계산한다. 세이브가 어긋나도 틀어지지 않는다.

- 하단 HUD 정리: 골드 표시를 없애고, 정지/킵/홀드를 스킬 칸 위로 올렸다.
  옆에 세로로 세워 두던 폭이 빠지면서 HP/MP/경험치 바가 106px -> 140px로 넓어졌다.
- 버프·디버프를 남은 시간(초)과 함께 슬롯에 표시한다. 예전에는 초상화 위 아이콘뿐이라 시간을 못 봤다.

## 스모크 수치 (반복 28)
- 단언: 영입 퀘스트 데이터 검사(참조 45x + 중복 0) 4 + 게이트 4 + 단계 종류 3 + 길잡이 6 +
  여정 8 + HUD 6 + 회귀(모든 창 / 전 존 / 세이브) 3 = 34 (누적 813). 콘솔 에러 0건.

## 반복 29 - 튜토리얼 실플레이 검수 / 스킬 아이콘 (2026-09-22)

- 세이브를 지우고 캐릭터 생성부터 길잡이 12단계까지 '실제 조작'으로 통과시켜 봤다.
  (방향키 입력 / Space 공격 / [킵] 버튼 클릭 / 스킬 습득 버튼 / 스킬 버튼 / 장착 버튼 /
   상점 판매 버튼 / 강화 버튼 / 영입 NPC 대화창 수락 / 텔레포트 / 시나리오 NPC 대화)
  12/12 통과 · 졸업 보상까지 확인. 그 과정에서 막히는 자리 두 개를 찾아 고쳤다.
- [고침] ⑦ '장비 갈아입기'에서 가방이 비어 진행이 막혔다. 장비 드랍률이 6%라 운이 나쁘면 몇 분씩
  걸리고, 떨어져도 조작 캐릭터가 못 끼는 물건이면 헛일이었다.
  -> 그 단계 동안만, 조작 캐릭터가 바로 낄 수 있는 장비 한 점을 확정으로 떨어뜨린다(3초 만에 해결).
- [고침] 강화가 통째로 깨져 있었다. starforceGear가 정의된 적 없는 이름(catchStar)을 돌려줘서,
  강화를 누를 때마다 골드·재료만 빠지고 ReferenceError가 났다. 튜토리얼 ⑨단계를 밟다 발견했다.
  -> 원래 의도대로 chanceTime(찬스 타임 사용 여부)을 돌려준다. 8연속 강화 무사 통과 확인.
- [고침] 여정의 '강화' 지표가 늘 0이었다. Gear의 필드는 star(단수)인데 stars로 읽고 있었다.

- 스킬·전용기·버프를 글자에서 아이콘으로 바꿨다(js/data/sprites_skills.js).
  이름 두 글자로 줄여 쓰던 하단 스킬칸이 9x9 도트 아이콘이 됐다.
  - 스킬이 100개가 넘어 하나씩 그리면 품질이 고르지 않다. '무엇을 하는 기술인가'를 21가지 모양으로
    나누고 색만 속성·역할에서 가져온다. 실제로 쓰이는 조합은 25가지.
  - [고침] 첫 판에서 smash가 램프처럼, meteor가 bolt와 똑같이, cleanse가 refresh와 똑같이 보였다.
    48px로 뽑아 눈으로 보고 네 개를 다시 그렸다(내려찍기 / 꼬리 달린 불덩이 / 정화 물방울 / 둘레 표시).

## 스모크 수치 (반복 29)
- 단언: 생성 흐름 5 + 길잡이 실조작 12 + 아이콘 8 + 강화 회귀 2 + 회귀(모든 창 / 전 존 / 세이브) 3
  = 30 (누적 843). 콘솔 에러 0건.

## 반복 30 - 광역 평타 = 쩔 자리 (2026-09-22)

- 광역 평타 계열을 늘렸다. 폴암만 있던 것을 원작 근거대로 둘 더 붙였다.
  - 장궁(원작 석궁 계열) 새지터부터 — "평타가 광역이라는 장점이 있어 병사작에서도 딜러로 쓰인다"
  - 속성 팔찌 로드 오브 계열 — "압도적으로 넓은 범위에 즉시시전"
- [고침] 반경이 너무 작아 '광역'이 이름뿐이었다. 같은 캐릭터로 장검/창만 바꿔 재 보니 잡은 수가
  19 대 18로 사실상 같았다. 사냥터의 실제 몹 간격을 재 보니 70px 안에는 평균 0.5마리뿐이었다
  (150px 1.3마리 / 190px 3마리). -> 반경을 130~200px로 다시 잡았다.
  재측정: 일반 사냥터 1.43배, 수련장 2.40배. 보스전에서는 이점 0이라 단일 계열도 살아 있다.
- 휘말린 쪽은 70%만 받게 했다(SPLASH_DAMAGE_MULT). 100%면 몹이 뭉친 곳에서 3배가 된다.
- [주의] A/B 측정이 한동안 엉뚱한 값을 냈다. availableStances는 '지금 든 무기 세트' 기준이라
  indexOf('sword')가 -1이 되어 두 번 다 창으로 돌고 있었다. 세트를 바꿔 재야 한다(activeSet).
- 수련장을 촘촘하게 깔았다(30 -> 48마리). 쩔 자리인데 몹이 흩어져 있으면 광역이 값을 못 한다.
  입장할 때 지금 광역 평타를 쓰는 캐릭터가 누구인지 채팅으로 알려 준다.
- 쩔 배지: 1레벨부터 광역인 8명(+파이터)에게만 붙인다. 처음엔 '언젠가 되는' 기준으로 했더니
  45명 중 26명에게 붙어서 표시가 뜻을 잃었다. 승급해야 열리는 18명은 툴팁으로만 알린다.

## 스모크 수치 (반복 30)
- 단언: 광역 계열 5 + 쩔 판정 5 + 배지 3 + 실측 A/B 2 + 회귀(모든 창 / 전 존 / 세이브) 3
  = 18 (누적 861). 콘솔 에러 0건.
